@model DrBAE.WdmServer.Models.RawDataModel

@{
    ViewData["Title"] = "Delete";
}

<h1>삭제</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RawData</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Sn)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Sn)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.AnalysisDataModels)
        </dt>
        <dd class="col-sm-10">
            @(Model.AnalysisDataModels.Select(x => x.Id).Select(x => $"{x} "))
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
